services:
  run-container:
    container_name: 42-Docker-DevEnv
    platform: linux/amd64
    image: minishell-dev:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
